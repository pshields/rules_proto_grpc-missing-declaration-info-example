import * as grpc from 'grpc';

// TODO Import the generated MyExampleServiceService symbol here

// Create the server instance
const server = new grpc.Server();

// TODO Tell the server about the service definition
// e.g. server.addService(MyExampleServiceService, ...)

// TODO Run the server in a loop until an exit signal is received
